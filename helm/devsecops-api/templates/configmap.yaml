apiVersion: v1
kind: ConfigMap

metadata:
  name: devsecops-api-config

data:
  LOG_LEVEL: {{ .Values.config.logLevel | quote }}
  ENVIRONMENT: {{ .Values.config.environment | quote }}
  UVICORN_PORT: {{ .Values.config.uvicornPort | quote }}
