apiVersion: v1
kind: Service

metadata:
  name: devsecops-api

spec:
  type: {{ .Values.service.type }}

  selector:
    app: devsecops-api

  ports:
    - name: http
      protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}